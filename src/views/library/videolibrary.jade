include ../api/videos

block scripts
	script.
		$(function(){
			$.ongaku.library.bind();
			$.ongaku.library.loadVideo();
			$.ongaku.library.fetch();
			$('.pending-list.wfull').removeClass("wfull");
		});
